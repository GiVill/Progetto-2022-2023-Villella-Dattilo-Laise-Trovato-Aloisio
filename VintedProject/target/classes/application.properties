#Impostazioni del DATABASE. In ordine:
# Path al DB,
# Username,
# Password,
# modalit� del database (pu� essere tra le altre cose: none, update, create, create-drop),
# mostra l'sql sul terminale,
# dialetto del DB
spring.datasource.url=jdbc:postgresql://localhost:15432/vinted
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.jpa.hibernate.ddl-auto=update
spring.datasource.show-sql=true
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

#mette il server in ascolto sulla porta 8010
server.port=8010

#Internazionalizzazione
spring.messages.basename=messages, i18n.list
spring.messages.fallback-to-system-locale=false

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

#Permette l'override dei Bean. Nel nostro progetto � RICHIESTO, altrimenti non parte
spring.main.allow-bean-definition-overriding=true

#Configurazione per l'email. In ordine:
# Server SMTP,
# porta del server SMTP,
# Username,
# Password,
# Autenticazione al server SMTP abilitata/disabilitata,
# Connessione col server SMTP in TLS
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=vintedproject09@gmail.com
spring.mail.password=ezmtzvwwxfrfkcha
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
